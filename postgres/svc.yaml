apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    app.kubernetes.io/instance: postgres
    app.kubernetes.io/name: postgres
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 5432
      protocol: TCP
      targetPort: 5432
  selector:
    app.kubernetes.io/instance: postgres
    app.kubernetes.io/name: postgres
